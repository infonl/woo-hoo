apiVersion: v1
kind: Service
metadata:
  name: {{ include "woo-hoo.fullname" . }}
  labels:
    {{- include "woo-hoo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "woo-hoo.selectorLabels" . | nindent 4 }}
